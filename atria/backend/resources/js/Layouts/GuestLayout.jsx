export default function GuestLayout({ children }) {
    return (
        <div>
            {/* Placeholder for styling/layout based on modele/layouts/authentication/sign-in/basic */}
            {children}
        </div>
    );
}
